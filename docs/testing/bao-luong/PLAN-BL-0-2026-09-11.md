# Kiểm tra implementation plan BL-0 — 2026-09-11

Trạng thái: **Chờ anh Tú duyệt.**

Plan: `docs/superpowers/plans/2026-09-11-bao-luong-bl0-workflow-foundation.md`

Phạm vi: duyệt kế hoạch, chưa apply migration và chưa sửa code.

## Kiểm tra tự động Codex đã chạy

- [x] Plan có 9 task; task logic có test đỏ/xanh, task UI/docs có typecheck/manual test và commit.
- [x] Không có `TBD`, `TODO`, thân test rỗng hoặc đường dẫn dependency chưa xác định.
- [x] Migration mới bắt đầu từ 049; không sửa migrations 001–048.
- [x] Có cổng dừng và file test riêng sau BL-0.
- [x] `git diff --check` không báo lỗi whitespace.

## Test 1 — Phạm vi BL-0

Xác nhận BL-0 chỉ làm nền đã chốt:

- Cấu hình quy trình trong Admin Web cho owner và MEVO.
- Cổng POS theo quán, không làm Pubu phải chờ xác nhận.
- Vòng đời Gọi nhân viên, chống spam và nút Đã xử lý.
- Siết thu tiền/Bỏ bàn về chủ quán.
- Entry Mini App `root/table`, tắt Mang về/Ship Bảo Lương ở cả UI và RPC.
- Chưa triển khai schema/form đặt bàn; phần đó bắt đầu ở BL-1.

## Test 2 — An toàn và hồi quy

Xác nhận plan có:

- PGlite test migration, RLS, RPC, rollback nguyên tử và quyền.
- Ma trận Pubu/Bảo Lương cho kênh bán và quy trình bếp.
- Backend guard để không thể gọi thẳng RPC lách capability.
- Timeout phiên theo cấu hình và không hardcode “6 giờ” ở UI dùng chung.
- Kiểm tra cả core lẫn hai worktree mini-app instance.

## Test 3 — Cổng thực thi

Xác nhận sau khi thực hiện Task 9:

- Tạo `docs/testing/bao-luong/SPRINT-BL-0.md`.
- `TESTING.md` chỉ nhận link/trạng thái.
- Dừng chờ `BL-0 PASS`, không tự chuyển sang BL-1.

Kết quả cần phản hồi: `PLAN BL-0 PASS` hoặc ghi rõ task cần sửa.
