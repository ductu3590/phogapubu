# Duyệt implementation plan — Sprint BL-2B

Ngày: 21/09/2026

Plan cần duyệt:
`docs/superpowers/plans/2026-09-21-bao-luong-bl2b-zalo-oa-owner-notifications.md`

## Trạng thái thực tế trước khi code

- OA ID Bảo Lương: đã có.
- Mini App ID Bảo Lương: đã có.
- OA access token: chưa có trên remote.
- App secret/webhook secret: chưa có trên remote.
- Recipient OA-scoped user ID của chủ quán: chưa có schema/onboarding.
- Existing `zns-notify` dành cho order và đang trả `success: true` dù provider có thể trả lỗi;
  BL-2B tạo sender riêng, chỉ coi `error = 0` là thành công.

## Test duyệt plan

1. BL-2B chỉ gồm onboarding người nhận chủ quán và thông báo booking khách mới; không kéo tin
   xác nhận khách, nhắc 60 phút hoặc preorder vào Sprint này.
2. Đồng ý dùng challenge gửi qua chính OA để lấy OA-scoped user ID, không cho nhập UID bằng tay.
3. Đồng ý outbox + Database Webhook + Edge Function; booking không rollback khi Zalo lỗi.
4. Đồng ý Task 3 không thể PASS bằng mock: phải nhập credential qua cockpit và nhận tin test thật.
5. Đồng ý mỗi Task 1–4 dừng riêng theo `docs/testing/bao-luong/SPRINT-BL-2B.md`.

→ Nếu đúng hướng, trả lời: `PLAN BL-2B PASS`.

✅ **PLAN BL-2B PASS** — anh Tú duyệt ngày 21/09/2026.
