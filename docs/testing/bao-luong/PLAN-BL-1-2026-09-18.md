# Kiểm tra implementation plan BL-1 — 2026-09-18

Trạng thái: **Chờ anh Tú duyệt.**

Plan: `docs/superpowers/plans/2026-09-18-bao-luong-bl1-reservation-foundation.md`

## Test 1 — Đúng phạm vi BL-1

- Có reservation/schema/RPC/audit, slot server-side, phân bổ, đổi/hủy/no-show, nhận khách và nối session/mâm.
- Không có UI khách/POS, OA/ZNS, in phiếu hay preorder: các phần này thuộc BL-2/BL-3.
- Không sửa `create_order` hoặc hành vi Pubu.

## Test 2 — An toàn dữ liệu và vận hành

- Customer dùng opaque token hash, không dùng Zalo UID/PIN để authorize.
- RLS chặn ghi trực tiếp; owner RPC lock bàn, check overlap/phiên mở và audit mọi quyết định.
- Slot/giới hạn tính server-side theo Asia/Ho_Chi_Minh; `is_accepting_orders` không chặn booking tương lai.
- Arrival idempotent tạo đúng một mâm mở gọi chung; chỉ `paid` complete booking.

## Test 3 — Cổng thực thi

- Migration 052–056, PGlite test đỏ/xanh và regression 049/050/050a.
- Có typed action chuẩn bị BL-2 nhưng chưa dựng UI sớm.
- Hoàn thành sẽ tạo `SPRINT-BL-1.md`, chỉ thêm link/trạng thái vào `TESTING.md`, dừng chờ PASS.

Kết quả cần phản hồi: `PLAN BL-1 PASS` hoặc ghi rõ task cần sửa.
