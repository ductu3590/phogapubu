# Dependencies
node_modules/
.pnp
.pnp.js

# Env files — KHÔNG commit credentials thật
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
.next/
out/
dist/
build/
.zmp/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Supabase local dev
supabase/.branches/
supabase/.temp/

# Local visual brainstorming companion
.superpowers/

# Git worktrees (isolated workspaces for agent-driven implementation)
.worktrees/
.claude/worktrees/

# Mini-app app-config.json là cấu hình riêng theo từng quán (title, v.v.) — không commit bản
# thật, chỉ commit app-config.example.json làm template. Xem scripts/create-mini-app-instance.sh.
mini-app/app-config.json

# Worktree lâu dài cho từng quán (mini-app-instances/<slug>/) — không phải worktree tạm của
# agent, nhưng cũng không nên commit nội dung checkout vào chính repo.
mini-app-instances/
